[
  {
    "image": "${account_id}.dkr.ecr.${region}.amazonaws.com/${service_name}:${image_tag}",
    "networkMode": "${task_network_mode}",
    "name": "${service_name}",
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "/ecs/${service_name}",
        "awslogs-region": "${region}",
        "awslogs-stream-prefix": "ecs"
      }
    },
    "portMappings": [
      {
        "protocol": "tcp",
        "containerPort": ${service_container_port}
      },
      {
        "protocol": "tcp",
        "containerPort": ${service_metric_port}
      },
      {
        "protocol": "tcp",
        "containerPort": ${rpc_container_port}
      }
    ],
    "dockerLabels": {
      "PROMETHEUS_EXPORTER_PORT": "${service_metric_port}",
      "PROMETHEUS_EXPORTER_JOB_NAME": "${service_name}"
    },
    "environment": [
      {
        "name": "API_DOMAIN",
        "value": "${env_api_domain}"
      },
      {
        "name": "WEB_DOMAIN",
        "value": "${env_web_domain}"
      },
      {
        "name": "COUNTRY_CODE",
        "value": "${env_country_code}"
      },
      {
        "name": "PARTY_ID",
        "value": "${env_party_id}"
      },
      {
        "name": "ISSUER",
        "value": "${env_issuer}"
      },
      {
        "name": "DB_USER",
        "value": "${env_db_user}"
      },
      {
        "name": "DB_PASS",
        "value": "${env_db_pass}"
      },
      {
        "name": "DB_HOST",
        "value": "${env_db_host}"
      },
      {
        "name": "DB_NAME",
        "value": "${env_db_name}"
      },
      {
        "name": "FCM_API_KEY",
        "value": "${env_fcm_api_key}"
      },
      {
        "name": "METRIC_PORT",
        "value": "${env_metric_port}"
      },
      {
        "name": "REST_PORT",
        "value": "${env_rest_port}"
      },
      {
        "name": "RPC_PORT",
        "value": "${env_rpc_port}"
      },
      {
        "name": "RECORD_EVSE_STATUS_PERIODS",
        "value": "${env_record_evse_status_periods}"
      },
      {
        "name": "SHUTDOWN_TIMEOUT",
        "value": "${env_shutdown_timeout}"
      }
    ]
  }
]
